<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      base-config allows cleartext so youtube_explode_dart's local proxy
      (http://127.0.0.1) works.  All real API / stream traffic is HTTPS
      by design — the Dart code only ever constructs https:// URLs for
      Saavn, YouTube, Piped, and Firebase endpoints.
    -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug configuration -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>

